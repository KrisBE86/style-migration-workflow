---
name: style-migration-workflow
description: Use when 用户要把目标图内容迁移到指定视觉风格中，尤其需要在仅主体、主体加风格化场景、主体加真实场景拼贴之间选择，并处理多人、多物体的编号、位置、遮挡、接地、相对大小和局部返修。
---

# 通用风格迁移工作流

## 核心原则

本 Skill 严格遵循“流程与风格分离”原则。

1. **流程层（SKILL.md）**：只负责分析目标图、确认需求、选择模式、调用模块、检查结果。**它不包含任何具体的画法参数（如数值、颜色、线条粗细）。**
2. **流程提示词层（references/workflow-blocks.md）**：存放内容与风格分离、主体地图、模式A/B/C等通用生成模块。
3. **返修提示词层（references/repair-blocks.md）**：存放只修改指定主体、接地/贴合、相对大小、锁定已通过对象等局部修正模块。
4. **风格层（references/styles/）**：每种风格作为一个独立文件夹存放，例如 `style-1-marker-doodle/style.md`。如果同目录存在 `reference.png`，将其作为视觉风格参考；如果存在 `notes.md`，同时读取参考图使用边界。本 Skill 只负责按用户选择读取，不临时改写风格内容。
5. **内容层（目标图）**：决定画谁、画什么场景。不继承参考图中的标识或人物。

## 适用场景与模式

**模式A：仅生成人物**
- 只生成指定的人物（或动物），不包含背景。
- 背景处理：白底留白，或纯色背景。

**模式B：生成人物+生成场景**
- 将目标图中的主体和背景整体转化为统一风格。
- 原图提供构图和内容，风格由所选模块决定。
- 背景概括主要空间和大物件，删除杂乱小物、文字、Logo、复杂纹理和真实光影。

**模式C：生成人物+真实场景**
- 保留目标图的真实背景，移除原人物，插入新生成的人物（类似拼贴）。
- 背景不经过风格化处理，保持原貌（或仅做极其轻微的弱化/修补）。

## 模式选择

| 用户想要 | 使用模式 | 关键判断 |
|---|---|---|
| 只要角色、头像、单体物件 | 模式A | 背景可删除，重点是主体风格化 |
| 整张图都变成同一种画风 | 模式B | 主体和背景一起重绘，真实背景不保留 |
| 像拼贴一样替换真实照片里的人或物 | 模式C | 真实背景保留，主体单独生成后再放回原位 |

当用户说“替换到真实场景”“保留原图背景”“把人物去掉换成新生成的”时，默认进入模式C。

## 生成前确认门禁（硬性阻断）

这是执行门禁，不是建议。未通过门禁时，本 Skill 的唯一允许输出是向用户提出确认问题。禁止调用任何图像生成、图像编辑、批量生成、合成、修图或导出工具。

在满足以下三项确认前，必须停止执行：

1. **风格选择**：用户明确选择 `references/styles/` 中的某个 Style 文件夹，或明确授权“由你选择风格”。如果用户给了参考图，必须询问它是唯一风格参考，还是要套用 `references/styles/` 中的已有 Style 文件夹。
2. **模式选择**：用户明确选择模式A、模式B或模式C，或明确授权“由你判断模式”。即使用户已经描述了保留或删除背景，也只能复述判断并等待用户确认，不能直接执行。
3. **关键特征**：用户明确说明不能过度简化、不能改形或不能替换的特征，如发型轮廓、眼镜、面具、纹身、特殊道具、宠物花色、服装图案等；或明确表示“没有特殊保留要求”。

如果缺少任意一项，必须只提问，不得继续分析到生成步骤，不得建立最终提示词，不得调用生成工具。

不得因为“看起来明显”“只有一个合理选择”“为了减少来回”“默认采用”“先生成草稿”“之后再改”“用户大概率想要”等理由跳过确认。任何替用户决定风格、模式或关键特征的行为都视为流程失败。

## 标准流程

0. **检查确认门禁**：先检查“生成前确认门禁”三项是否全部满足。未全部满足时，只向用户提问并停止，不进入后续步骤。
1. **建立主体地图**：记录主体数量、编号、位置、姿态、动作、头部朝向、主色、关键特征、前后遮挡关系、接地线、相对大小。多人或多物体场景必须固定编号，后续持续使用同一编号。
2. **检索所需模块**：根据用户选定的模式，读取 `references/workflow-blocks.md` 中对应的模式模块；根据用户选定的风格，读取 `references/styles/` 中对应风格文件夹的 `style.md`。如果该文件夹包含 `reference.png`，将其作为视觉风格参考；如果包含 `notes.md`，同时读取并遵守。需要局部返修时，再读取 `references/repair-blocks.md` 中对应模块。不要让参考图内容污染目标图内容。
3. **确认模式并执行**：
   - **模式A**：调用 **【模式A：仅主体生成】** 或 **【模式A：单个主体生成】**，结合所选 Style 文件夹生成白底或纯色底图。
   - **模式B**：调用 **【模式B：主体加风格化场景】**，结合所选 Style 模块整体重绘目标图。
   - **模式C**：先按编号逐个生成主体素材，必要时做角色表；用户确认或主体稳定后，优先进入“模式C图层合成流程”，把已确认主体作为固定图层放回真实背景。只有在用户明确接受整体生成式编辑、或图层合成无法处理时，才调用 **【模式C：真实场景替换合成】** 直接生成整图。
4. **锁定已通过部分**：用户认可某个编号、姿态、头部比例、背景或整体构图后，将其作为锁定项。后续返修只改变用户点名的问题。
5. **针对性迭代**：用户指出问题时，**只调用** `references/repair-blocks.md` 中的 **【只修改指定主体】、【接地/贴合修复】、【相对大小修正】、【头部不变身体调整】** 等局部修正模块。
6. **最终检查**：检查是否符合所选模式的逻辑（人物数量、位置、遮挡、接地、残留物检查），检查画风是否严格符合所选 Style 模块。

## 模式C的关键经验

模式C最容易失败的地方不是风格，而是空间关系。优先检查这些问题：

- **主体是否太整体化**：多人场景如果一次生成结构不稳定，改为逐个生成主体，再合成。
- **原主体是否残留**：脸、手、鞋、衣服边缘和头发边缘最容易露出旧图残影。
- **是否悬浮**：脚底、坐姿支撑点、手持物接触点必须落在原图对应位置。
- **相对大小是否错位**：风格化可以改变绝对头身比，但不能颠倒原图中的高矮、远近、坐站关系。
- **是否误改已通过对象**：用户说“只改第三个”“第四保持不变”时，必须锁定其他编号。
- **是否为了真实比例牺牲风格**：Q版或简化风格不必追求真人身高；只保留群体间的相对差异。

## 模式C图层合成流程

当用户选择模式C，且已经确认逐个生成的主体素材后，应优先使用类似 PS 的图层合成流程，而不是继续用生成式编辑反复重画整张图。

推荐流程：

1. **固定主体素材**：把用户确认过的每个主体素材作为锁定图层。后续只允许移动、缩放、裁切、遮罩、去边或调整叠放顺序，不再重新生成主体，除非用户明确要求重画某个编号。
2. **制作干净背景底板**：从目标图中只移除原主体，修补被遮挡的真实背景、椅子、地面、墙面、桌面等。文字、Logo、主要背景物、真实光影和原图裁切必须保持不变。
3. **生成真实透明图层**：不要相信预览中的棋盘格一定代表 alpha 通道。必须检查素材是否真的包含透明通道；如果素材是 RGB 或白底/棋盘底已经烘进图里，应先抠底并保存为真正透明的 PNG。
4. **去白边/去脏边**：对主体图层边缘做去白边处理，尤其是从白底、浅灰棋盘底或抗锯齿边缘抠出的素材。必要时收缩 1-2 像素遮罩，或降低浅灰边缘 alpha，避免贴到暗背景上出现白色描边。
5. **按坐标合成**：用代码或图层工具把每个主体按编号贴回背景底板。每个主体应记录中心点、缩放高度、接地点或坐姿支撑点、遮挡关系和层级顺序。多人场景要保持编号、左右顺序、相对大小和姿态不变。
6. **用标注图确认空间线**：遇到“悬浮”“没坐上去”“没踩实”等问题时，先输出带辅助线的标注图，明确地面线、凳面线、桌面线、屁股线、脚底线或手持接触点。用户确认线的位置后，再调整图层参数。
7. **局部迭代只改图层参数**：用户说某个主体太高、太低、太大、太小、偏左或偏右时，只改该编号的图层坐标、缩放或锚点；不要重新生成主体，不要让其他编号漂移。
8. **最终导出**：导出无标注最终稿，同时可保留一张带辅助线和图层框的核对稿。最终稿不得包含辅助线、编号标签、棋盘格、白边、旧主体残影或未修补背景。

图层合成优先级高于整图生成式编辑，因为整图编辑即使提示“保持不变”，也可能导致已确认主体的五官、发型、服装、比例和线条发生细微漂移。用户已经确认主体后，这种漂移应视为需要避免的问题。

### 图层脚本调用规则

满足以下任一条件时，必须优先考虑使用 `references/scripts/` 中的通用脚本，而不是继续手工描述合成或整图生成式编辑：

- 用户已经确认了一个或多个主体素材，并要求放回真实背景。
- 用户指出“人物变了”“每次改动人物都漂移”“像 PS 合成”“只改位置/大小/坐标/接触点”。
- 用户要求修正某个已确认主体的高低、左右、大小、坐姿、接地、遮挡或白边。
- 需要输出带辅助线、图层框、编号或接触线的核对图。

可用脚本：

- `make_character_sheet.py`：把多个已确认主体素材按编号排成角色表，便于用户确认和后续引用。
- `layer_composite.py`：把固定主体图层按 CSV 坐标合成到干净背景底板上，并可输出带图层框的核对稿；当素材没有真实 alpha 时，可启用去底和去白边处理。

调用要求：

1. 使用这些脚本前，先读取 `references/scripts/README.md`。
2. 为 `layer_composite.py` 准备 placement CSV，至少记录每个主体图层的素材路径、水平中心、缩放高度、场景锚点线和主体自身锚点比例。
3. 每次用户确认某个图层位置后，更新 placement CSV 或等价参数，并将该编号视为锁定项。
4. 当用户只反馈某个编号的问题时，只改该编号的 placement 参数或遮罩处理，不重画主体，不移动其他编号。
5. 脚本只是执行图层合成流程的工具，不定义画风，不替代主体地图和用户确认。

## 模式C坐姿/接地判断

坐姿、骑乘、趴伏或手扶物体时，不能只看脚底。必须先判断真正的支撑点：

- **坐姿人物**：屁股或上大腿下缘是主要接触点，必须落在凳面、椅面、台阶或地面坐点上；脚只是辅助接地。
- **站立人物**：鞋底或脚底是主要接触点，必须落在地面线或台阶面上。
- **手扶/手持物**：手掌、手指或物件握点必须与对应物体边缘接触，不能悬空。
- **多人同坐**：先统一确认座面水平线，再为每个主体设置各自的臀部锚点。不要把靠背横杆、前横梁、阴影线或脚边线误判为座面。

如果用户指出“这里才是凳面”“这是屁股线”等坐标反馈，应把它转化为明确的图层锚点调整：例如将主体的臀部锚点对齐到已确认的座面线，而不是重新生成整张图。

## 核心规则

- 未通过“生成前确认门禁”前，禁止调用任何图像生成、图像编辑、批量生成、合成、修图或导出工具。
- 参考图只决定风格，绝不继承其内容。
- 目标图决定主体内容、空间关系和构图。
- 风格参数只从选定风格模块读取，不在 `SKILL.md` 临时发明。
- 模式C中，用户确认主体素材后，应优先使用固定图层合成；除非用户明确要求，不要用整图生成式编辑反复重画已确认主体。
- 每次修改后，只简述修改了哪个编号、重点修正了什么、哪些部分保持不变。
